<h1>User Settings</h1>
<form id="userSettings" ng-controller="SettingsController">
    <div class="formInputGroup">
        <input type="checkbox" id="showSongInUrl" {{#if settings.showSongInUrl}}checked{{/if}}>
        <label for="showSongInUrl">Show song ID in URL bar</label><br>
        <input type="checkbox" id="showSongInTitle" {{#if settings.showSongInTitle}}checked{{/if}}>
        <label for="showSongInTitle">Show song name in page title</label><br>
        <input type="checkbox" id="debugMode" {{#if settings.debugMode}}checked{{/if}}>
        <label for="debugMode">Show debug information</label><br>
    </div>
    <div class="formInputGroup">
        <label for="shuffleMode">Shuffle Mode</label><br>
        <select id="shuffleMode">
            <option selected>Random</option>
            <option>Favour liked songs</option>
            <option>Favour unheard songs</option>
        </select>
    </div>
    <div class="formInputGroup">
        <h2>API Access</h2>
        <h3>Below is your key for accessing the API. Keep this secret as it has access to your full account. Regenerating the key will revoke the old one!</h3>
        <input readonly type="text" placeholder="You have not yet generated a key" id="apiKey" ng-model="settings.apiKey" class="fancyInput"><button class="happyButton" id="regenerateKey()" title="Regenerate key" ng-click="regenerateKey()"><i class="fa fa-repeat"></i></button>
    </div>
    <input type="submit" value="Save" class="happyButton">
</form>