<div id="allSongs" class="songList visible fancyScroll" ng-controller="SongController">
    {{#if songs}}
        {{#each songs}}
            {{>song}}
        {{/each}}
    {{else}}
        <div class="softError">There doesn't seem to be anything here.</div>
    {{/if}}

</div>