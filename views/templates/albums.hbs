<div class="albumList fancyScroll" ng-controller="SongController">
    {{#if albums}}
        {{#each albums}}
            {{>album}}
        {{/each}}
    {{else}}
        <div class="softError">There doesn't seem to be anything here.</div>
    {{/if}}
</div>